#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int perevod(int);
int main
{
	int x;
	printf("vvedite chislo\n");
	scanf("%d",&x);
	printf("vvedite kolichestvo cifr\n");
	scanf("%d",&n)
	int A[n];
	perevod(x);
	for(int i=0;i<n;i++)
	{
		printf("%d";A[i]);
	};
}
int perevod (int x1)
{
	if(x1<1)
	{
		return 0;
	};
	if(x1%2==0)//глянуть как шмонать остаток от деления
	{
		x1=x1%2
		A[n-i]=0;
	};
	if(x1%2!=0)
	{
		x1=x1%2;
		A[n-i]=1;
	}
}//пиздец какой то ну можно короче ввести еще i какое то и там блять муть эту делать
